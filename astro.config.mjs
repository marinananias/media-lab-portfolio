import { defineConfig } from "astro/config";

export default defineConfig({
  site: "https://medialabportfolio.marinaananias.com",
  // If you ever served this under a subpath instead of a subdomain,
  // you'd use `base`, but with a subdomain you can keep it simple.
});
